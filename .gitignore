# Hugo build output
/public/
/resources/_gen/
.hugo_build.lock

# OS
.DS_Store

# Local Claude Code settings
.claude/

# Local environment variables (copy from .env.example, never commit)
.env

# Downloaded fonts (local tooling only, not committed)
/fonts/

# Python utility venv — created by `task scripts:setup`, uses Python 3.13 from brew
# All Python tooling lives under scripts/ to keep the repo root clean
/scripts/.venv/

# Task runner cache
/.task/

