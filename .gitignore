# Hugo build output
/public/
/resources/_gen/
.hugo_build.lock

# OS
.DS_Store

# Local Claude Code settings
.claude/

# Local environment variables (copy from .env.example, never commit)
.env

# Downloaded fonts (local tooling only, not committed)
/fonts/

